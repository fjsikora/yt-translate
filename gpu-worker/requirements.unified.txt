# Unified Self-hosted GPU Worker Requirements
# Combined dependencies for all AI models in single image
# All versions pinned for reproducibility
#
# Estimated model storage requirements:
# - Whisper large-v3: ~3GB
# - Demucs htdemucs: ~0.5GB
# - pyannote.audio: ~0.3GB
# - Chatterbox TTS: ~2GB
# - Qwen3-8B GGUF: ~5GB (Q4_K_M quantization)
# Total models: ~11GB

# =============================================================================
# Core ML Frameworks (CUDA 12.4 compatible)
# =============================================================================
torch==2.5.1
torchaudio==2.5.1
# Note: Using 2.5.1 to avoid PyTorch 2.6 weights_only=True breaking pyannote

# =============================================================================
# Speech-to-Text (Whisper)
# =============================================================================
faster-whisper>=1.0.0
# Using faster-whisper for better performance with CTranslate2 backend
# (Replaces openai-whisper which was used for model download only)

# =============================================================================
# Audio Source Separation (Demucs)
# =============================================================================
demucs==4.0.1
# Uses torch.hub for model loading

# =============================================================================
# Speaker Diarization (pyannote.audio)
# =============================================================================
pyannote.audio==3.3.2
# Requires HuggingFace token for model access

# =============================================================================
# Text-to-Speech (Chatterbox)
# =============================================================================
# NOTE: chatterbox-tts is installed separately with --no-deps in Dockerfile
# because it pins torch==2.6.0 which breaks pyannote
# Below are chatterbox dependencies (excluding torch/torchaudio)
soundfile>=0.12.0
librosa==0.11.0
s3tokenizer
transformers==4.46.3
diffusers==0.29.0
resemble-perth==1.0.1
conformer==0.3.2
safetensors==0.5.3
spacy-pkuseg
pykakasi==2.3.0
pyloudnorm
omegaconf
# Note: chatterbox-tts requires numpy<1.26.0

# =============================================================================
# LLM Translation (Qwen via llama.cpp)
# =============================================================================
llama-cpp-python>=0.2.0
# Note: Will be installed with CUDA support from custom index

# =============================================================================
# Shared Dependencies
# =============================================================================
huggingface_hub>=0.20.0
numpy<1.26.0

# =============================================================================
# API & Infrastructure
# =============================================================================
self-hosted>=1.7.4
fastapi>=0.109.0
uvicorn>=0.27.0
pydantic>=2.0.0

# =============================================================================
# Storage & HTTP
# =============================================================================
supabase>=2.11.0
httpx>=0.27.0

# =============================================================================
# Utilities
# =============================================================================
python-dotenv>=1.0.1
scipy>=1.14.0
psutil>=5.9.0
