# Ralph Progress Log

## 2026-01-20 10:00:35 - New Session (branch: ralph/runpod-deployment)

## US-001: Archive cloud-based translation code - COMPLETE

**Date:** 2026-01-20 10:05
**Files changed:**
- archives/cloud_translate.py (moved from root)
- archives/requirements-cloud.txt (moved from root)
- archives/README.md (created)
- requirements.txt (removed replicate, openai deps)

**Implementation notes:**
- Moved cloud_translate.py and requirements-cloud.txt to archives/ directory
- Created comprehensive README.md explaining what's archived and why
- Updated requirements.txt to remove cloud API dependencies (replicate, openai)
- Kept httpx as it's needed for RunPod API calls
- Syntax and import checks pass

**Learnings:**
- archives/ directory already had dated subdirectories from previous Ralph sessions
- mypy not installed in venv, using py_compile for syntax checking
- Git correctly detects file renames when staging both deletion and new file

## US-002: Create translate_video function interface - COMPLETE

**Date:** 2026-01-20 11:00
**Files changed:**
- translate.py (added translate_video function, TypedDict types, refactored main)
- config.py (added WHISPER_SAMPLE_RATE=16000, BACKGROUND_VOLUME=1.0)

**Implementation notes:**
- Created translate_video(video_url, target_lang, output_dir, progress_callback) function
- Added TranslationResult TypedDict with output_url, duration, segments_count, speakers_count
- Added ProgressInfo TypedDict with stage, progress_pct, detail fields
- Progress callback calculates percentage based on 8 pipeline stages
- NoOpTracker class handles functions requiring tracker without Rich console
- main() now calls translate_video() with a callback that bridges to Rich display
- Uses config values: WHISPER_SAMPLE_RATE, CHATTERBOX_SAMPLE_RATE, VOICE_SAMPLE_DURATION_SECONDS, BACKGROUND_VOLUME
- Fixed CUDA_VISIBLE_DEVICES to not override if already set (for RunPod)

**Learnings:**
- TypedDict provides good type hints without runtime overhead
- Callback pattern allows decoupling Rich display from core logic
- Config.py already had CHATTERBOX_SAMPLE_RATE and VOICE_SAMPLE_DURATION_SECONDS
- Return field should be output_url per acceptance criteria (not output_path)

## US-003: Integrate Qwen3-32B for translation - COMPLETE

**Date:** 2026-01-20 12:30
**Files changed:**
- llm_translate.py (new module)
- translate.py (import and use llm_translate)

**Implementation notes:**
- Created llm_translate.py with translate_segments_llm() function
- Uses transformers library with BitsAndBytesConfig for 4-bit quantization
- Model name: Qwen/Qwen3-32B with nf4 quantization and double quant
- Global model/tokenizer cache (_llm_model, _llm_tokenizer) to load once and reuse
- Batches segments using LLM_MAX_SEGMENTS_PER_BATCH (50) with LLM_BATCH_OVERLAP (5)
- Build structured prompts with [N] numbered format for easy parsing
- Uses Qwen chat template with enable_thinking=False for faster response
- Graceful fallback: if LLM fails to load or parse, uses Google Translate per-segment
- translate.py's translate_segments() now delegates to translate_segments_llm()
- Progress callback bridges from llm_translate to tracker.update_detail()

**Learnings:**
- Config.py already had all LLM constants (LLM_MAX_SEGMENTS_PER_BATCH, LLM_BATCH_OVERLAP, LLM_MAX_TOKENS, LLM_TEMPERATURE)
- BitsAndBytesConfig requires torch and specific CUDA setup
- Qwen3 models support chat template with thinking mode that can be disabled
- Regex parsing with numbered format [N] is robust for extracting translations
- Import check in py_compile is sufficient; full import requires venv dependencies

## US-004: Create RunPod handler - COMPLETE

**Date:** 2026-01-20 14:00
**Files changed:**
- runpod/__init__.py (new package)
- runpod/handler.py (new module)

**Implementation notes:**
- Created runpod/handler.py with handler(job) function as RunPod entrypoint
- validate_input() validates video_url (required), target_lang (required), voice_sample_url (optional)
- Uses translate_video() from translate.py with progress_callback
- Progress reporting via runpod.serverless.progress_update() for real-time job status
- Uploads result to Supabase Storage using existing db.py functions
- Generates 24-hour signed URL for result download (SIGNED_URL_EXPIRATION_LONG from config)
- Returns structured output: status, output_url, duration, segments_count, speakers_count
- Error handling: ValueError for input validation, RuntimeError with traceback for unexpected errors
- Cleanup: work directory deleted after job completion (even on failure)
- refresh_worker=True ensures clean state between jobs (important for ML models)

**Learnings:**
- db.py already has upload_translation_to_storage() and get_translation_signed_url()
- config.py has SIGNED_URL_EXPIRATION_LONG = 86400 (24 hours)
- RunPod SDK: runpod.serverless.start() with handler config, progress_update(job, msg)
- sys.path.insert needed to import from parent directory in handler.py

## US-005: Create RunPod Dockerfile - COMPLETE

**Date:** 2026-01-20 15:00
**Files changed:**
- runpod/Dockerfile (new file)

**Implementation notes:**
- Base image: nvidia/cuda:12.4.0-runtime-ubuntu22.04 for GPU support
- System packages: python3.11, ffmpeg, git, wget via apt-get
- Python dependencies from main requirements.txt + RunPod-specific additions
- Additional deps: runpod, supabase, transformers, accelerate, bitsandbytes, scipy, stripe
- Whisper medium model pre-downloaded via `whisper.load_model('medium')`
- Demucs htdemucs model pre-downloaded via `torch.hub.load()`
- Entrypoint: CMD ["python", "runpod/handler.py"]
- Dockerfile validated with `docker build --check` (no warnings)

**Learnings:**
- nvidia/cuda images require specific version tags (12.4.0-runtime-ubuntu22.04)
- Pre-downloading models at build time reduces cold start time on RunPod
- Whisper model caches to ~/.cache/whisper, Demucs to ~/.cache/torch/hub
- Using --no-cache-dir for pip reduces image size
- Dockerfile approach: install base requirements.txt then add RunPod-specific deps
- US-006 will create dedicated runpod/requirements.txt for cleaner dependency management

## US-006: Create requirements-runpod.txt - COMPLETE

**Date:** 2026-01-20 16:00
**Files changed:**
- runpod/requirements.txt (new file)
- runpod/Dockerfile (updated to use new requirements)

**Implementation notes:**
- Created dedicated runpod/requirements.txt with all ML dependencies
- All versions pinned for reproducibility:
  - torch==2.5.1, torchaudio==2.5.1 (stable CUDA 12.4 compatible)
  - openai-whisper==20240930 (latest stable)
  - demucs==4.0.1, pyannote.audio==3.3.2, chatterbox-tts==0.1.6
  - transformers==4.47.1, accelerate==1.2.1, bitsandbytes==0.45.0 (for Qwen3-32B)
  - runpod==1.7.4, supabase==2.11.0, httpx==0.27.2
- numpy pinned to >=1.26.0,<2.0.0 for compatibility
- Updated Dockerfile to use dedicated requirements file instead of scattered installs
- Removed redundant pip install commands from Dockerfile
- Syntax checks pass, Dockerfile validates without warnings

**Learnings:**
- pip index versions <package> gives quick access to available versions
- Keeping numpy <2.0 avoids compatibility issues with older packages
- pyannote.audio 4.x has breaking changes; 3.3.2 is the last stable 3.x
- Consolidating requirements makes Dockerfile cleaner and more maintainable
- torch/torchaudio versions must match for compatibility

## US-007: Create RunPod client in API gateway - COMPLETE

**Date:** 2026-01-20 17:00
**Files changed:**
- runpod_client.py (new module)

**Implementation notes:**
- Created runpod_client.py with async functions for RunPod Serverless API
- submit_job(video_url, target_lang, voice_sample_url=None) returns job_id string
- get_job_status(job_id) returns JobStatus TypedDict with id, status, output, error
- cancel_job(job_id) returns bool indicating success
- Uses RUNPOD_API_KEY and RUNPOD_ENDPOINT_ID environment variables
- Custom RunPodError exception with status_code and details attributes
- Helper function map_runpod_status_to_api() maps RunPod statuses to existing API format
- Proper error handling: 401 (auth), 404 (not found), 429 (rate limit), 5xx (server error)
- TypedDict types for JobInput, JobOutput, JobStatus for type hints

**Learnings:**
- RunPod API endpoints: /run (submit), /status/{id} (poll), /cancel/{id} (cancel)
- RunPod statuses: IN_QUEUE, IN_PROGRESS, COMPLETED, FAILED, CANCELLED
- httpx.AsyncClient is preferred for async HTTP in FastAPI contexts
- httpx already available in project requirements (used by other modules)
- US-008 will use this client to update the Railway API endpoints

## US-008: Update Railway API to use RunPod - COMPLETE

**Date:** 2026-01-20 18:00
**Files changed:**
- server.py (new file - FastAPI gateway)
- Procfile (updated to use server:app)

**Implementation notes:**
- Created server.py as the new Railway API gateway
- POST /translate endpoint calls runpod_client.submit_job()
- GET /status/{job_id} endpoint calls runpod_client.get_job_status()
- Used map_runpod_status_to_api() for status mapping (pending/processing/completed/failed)
- JobStatus model matches old cloud_translate.py format for backwards compatibility
- Added POST /cancel/{job_id} endpoint for job cancellation
- Updated Procfile to use server:app instead of cloud_translate:app
- No Replicate code in active endpoints - all delegated to RunPod

**Learnings:**
- FastAPI async endpoints work seamlessly with httpx async client
- Progress percentage simplified to fixed values (0/50/100) since RunPod doesn't expose granular progress
- Procfile change is critical for Railway deployment to use new server
- Old cloud_translate.py remains in archives/ for reference but is no longer active

## US-009: Create docker-compose for local development - COMPLETE

**Date:** 2026-01-20 19:00
**Files changed:**
- docker-compose.yml (new file)

**Implementation notes:**
- Created docker-compose.yml for local development with GPU support
- Worker service builds from runpod/Dockerfile
- GPU configuration using deploy.resources.reservations.devices for nvidia-container-toolkit
- Environment variables loaded from .env file via env_file directive
- Named volumes for model caching: hf_cache, torch_cache, whisper_cache
- Volume mount for output directory: ./output:/tmp/yt-translate-output
- Port 8000 exposed for local testing with curl
- Added healthcheck for container status monitoring
- shm_size set to 8gb for PyTorch DataLoader compatibility
- restart policy: unless-stopped
- docker compose config validates without errors

**Learnings:**
- Docker Compose v2 GPU syntax: deploy.resources.reservations.devices
- Volume names can be customized with 'name' attribute for easier identification
- shm_size important for PyTorch to avoid DataLoader worker killed errors
- env_file loads variables, environment overrides or adds to them
- Container healthcheck helps identify when worker is ready

## US-010: Add health check and monitoring - COMPLETE

**Date:** 2026-01-20 20:00
**Files changed:**
- runpod/handler.py (added health check and monitoring functions)
- runpod/requirements.txt (added psutil dependency)

**Implementation notes:**
- Added check_gpu_availability() function to report GPU info (availability, count, names, CUDA version)
- Added check_loaded_models() function to check if ML packages are importable (whisper, demucs, pyannote, chatterbox, transformers)
- Added get_memory_usage() function to report RAM and GPU memory usage in GB
- Added log_memory_usage() helper for consistent memory logging at different stages
- Added log_startup_info() that runs at worker startup with full health report
- Handler now logs memory before and after job processing
- Handler now includes job_duration_seconds in response for all jobs (success and failure)
- Handler now includes error_trace field in failed job responses with full traceback
- Added psutil==5.9.8 to requirements for memory monitoring
- Syntax checks pass

**Learnings:**
- psutil.virtual_memory() provides system RAM stats
- torch.cuda.memory_allocated() vs memory_reserved() - allocated is actual usage, reserved is cached
- torch.cuda.get_device_properties(i).total_memory gives total GPU VRAM
- Import checks for ML packages are fast and don't load models into memory
- Detailed error traces help debugging failed jobs in RunPod logs

## ALL USER STORIES COMPLETE (Previous PRD)

All 10 user stories have been implemented and pass their acceptance criteria.
The RunPod deployment migration is complete.

---

# New PRD: runpod-mcp-deployment

## 2026-01-20 - Session Start

Working on new PRD to deploy using RunPod GitHub integration and MCP tools.

## US-002: Create Audio Worker Template - BLOCKED

**Date:** 2026-01-20
**Status:** BLOCKED - Awaiting permissions

**Blocker:**
- RunPod MCP tools not authorized (mcp__runpod__list-templates, mcp__runpod__create-template)
- Cannot create templates programmatically without MCP tool permissions
- User needs to grant RunPod MCP tool permissions in Claude Code

**Prerequisites verified:**
- Branch: ralph/runpod-deployment (correct)
- Dockerfile exists: runpod/audio/Dockerfile
- Handler exists: runpod/audio/handler.py
- Requirements exist: runpod/audio/requirements.txt
- GitHub integration completed (US-001 passed)

**Plan once permissions granted:**
1. List existing templates to check for duplicates
2. Create template with:
   - Name: yt-translate-audio
   - Image from GitHub: runpod/audio/Dockerfile
   - Container disk: 50GB
   - Volume: 20GB at /runpod-volume
   - Env vars: HUGGINGFACE_TOKEN, SUPABASE_URL, SUPABASE_SERVICE_KEY

---

## 2026-01-20 15:XX - Iteration Attempt (Still Blocked)

**Status:** US-002 still blocked on RunPod MCP permissions

**Attempted:**
- Called mcp__runpod__list-templates
- Received: "Claude requested permissions to use mcp__runpod__list-templates, but you haven't granted it yet."

**Verified:**
- All prerequisite files exist:
  - runpod/audio/Dockerfile
  - runpod/audio/handler.py
  - runpod/audio/requirements.txt
- Branch correct: ralph/runpod-deployment

**Action Required:**
User must grant RunPod MCP tool permissions in Claude Code settings/prompt.
Without these permissions, this iteration cannot proceed.

**Next Story After US-002:** US-003 (Create Chatterbox Worker Template) - also requires RunPod MCP tools

---

## 2026-01-20 ~21:XX - Iteration Attempt #3 (Still Blocked)

**Status:** US-002 still blocked on RunPod MCP permissions

**Attempted:**
- Called mcp__runpod__list-templates
- Received: "Claude requested permissions to use mcp__runpod__list-templates, but you haven't granted it yet."

**Verified:**
- Branch correct: ralph/runpod-deployment
- All prerequisite files exist:
  - runpod/audio/Dockerfile
  - runpod/audio/handler.py
  - runpod/audio/requirements.txt
  - runpod/chatterbox/Dockerfile
  - runpod/chatterbox/handler.py
  - runpod/chatterbox/requirements.txt
  - runpod/vllm/README.md
  - runpod/vllm/client.py

**Blocker:**
RunPod MCP tools require user permission grant. The tools available but not authorized:
- mcp__runpod__list-templates
- mcp__runpod__create-template
- mcp__runpod__create-endpoint
- mcp__runpod__list-endpoints

**Action Required:**
User must grant RunPod MCP tool permissions. When the permission prompt appears, click "Allow" or "Grant" to enable the tools.

---

## 2026-01-20 ~XX:XX - Iteration Attempt #4 (Still Blocked)

**Status:** US-002 still blocked on RunPod MCP permissions

**Attempted:**
- Called mcp__runpod__list-templates
- Received: "Claude requested permissions to use mcp__runpod__list-templates, but you haven't granted it yet."

**Blocker:**
RunPod MCP tools require explicit user permission grant. The tools are available in the system but not yet authorized:
- mcp__runpod__list-templates
- mcp__runpod__create-template
- mcp__runpod__create-endpoint
- mcp__runpod__list-endpoints

**Action Required:**
User must grant RunPod MCP tool permissions. When Claude requests permission to use a RunPod tool, click "Allow" or "Grant" in the permission dialog.

---

## US-002: Create Audio Worker Template - COMPLETE

**Date:** 2026-01-20
**Files changed:**
- prd.json (updated passes to true)

**Implementation notes:**
- RunPod MCP tools now authorized and working
- Created template via mcp__runpod__create-template
- Template ID: k0iwwtx14x
- Template name: yt-translate-audio
- Image: ghcr.io/fsikora/yt-translate/audio:latest
- Container disk: 50GB
- Volume: 20GB at /workspace (RunPod default mount path)
- Environment variables configured: HUGGINGFACE_TOKEN, SUPABASE_URL, SUPABASE_SERVICE_KEY
- Docker start command: ["python", "runpod/audio/handler.py"]

**Learnings:**
- RunPod MCP create-template ignores volumeMountPath and uses /workspace (their default)
- Environment variables must be set via update-template after initial creation
- isServerless=true is important for serverless templates
- Template ports default to 8888/http and 22/tcp even for serverless

## US-003: Create Chatterbox Worker Template - COMPLETE

**Date:** 2026-01-20
**Files changed:**
- prd.json (updated passes to true)

**Implementation notes:**
- Created template via mcp__runpod__create-template
- Template ID: 0m5zlkzse8
- Template name: yt-translate-chatterbox
- Image: ghcr.io/fsikora/yt-translate/chatterbox:latest
- Container disk: 30GB
- Volume: 10GB at /workspace (RunPod default mount path, ignores /runpod-volume)
- Environment variables configured via update-template: SUPABASE_URL, SUPABASE_SERVICE_KEY
- Docker start command: ["python", "runpod/chatterbox/handler.py"]

**Learnings:**
- Same pattern as audio template - create first, then update to add env vars
- Chatterbox doesn't need HUGGINGFACE_TOKEN (only SUPABASE vars)
- RunPod templates now ready: audio (k0iwwtx14x), chatterbox (0m5zlkzse8)

---

## US-004: Configure vLLM Endpoint - BLOCKED

**Date:** 2026-01-20
**Status:** BLOCKED - RunPod account balance insufficient

**Progress Made:**
- Researched RunPod's official vLLM Worker template
- Docker image: runpod/worker-v1-vllm:stable-cuda12.1.0
- Created template successfully with ID: gxd9dfcy8l
- Template configured with:
  - Model: Qwen/Qwen3-32B
  - MAX_MODEL_LEN: 8192
  - GPU_MEMORY_UTILIZATION: 0.95
  - TENSOR_PARALLEL_SIZE: 1
  - HF_TOKEN: (placeholder for user to set)
  - Container disk: 100GB
  - Volume: 100GB at /workspace

**Blocker:**
- Endpoint creation failed with error: "You must have at least $0.01 in your account balance to create an endpoint."
- RunPod requires minimum balance to create serverless endpoints

**OpenAI-Compatible API (documented for when endpoint is created):**
- Base URL: https://api.runpod.ai/v2/<ENDPOINT_ID>/openai/v1
- Endpoints:
  - POST /v1/chat/completions (streaming and non-streaming)
  - GET /v1/models

**Action Required:**
User must add funds to RunPod account (minimum $0.01) to create the endpoint.

**Learnings:**
- RunPod's official vLLM worker image: runpod/worker-v1-vllm:stable-cuda12.1.0
- vLLM worker provides OpenAI-compatible API at /openai/v1 path
- Qwen3-32B requires A100 80GB GPU (large 32B parameter model)
- Template creation works without balance, but endpoint creation requires funds
- Templates now: audio (k0iwwtx14x), chatterbox (0m5zlkzse8), vllm (gxd9dfcy8l)

---

## 2026-01-20 - Iteration Attempt (US-004 Still Blocked)

**Status:** US-004 remains blocked - RunPod account balance insufficient

**Verified:**
- Branch correct: ralph/runpod-deployment
- Templates exist and are properly configured:
  - yt-translate-audio (k0iwwtx14x)
  - yt-translate-chatterbox (0m5zlkzse8)
  - yt-translate-vllm-qwen (gxd9dfcy8l)
- Attempted endpoint creation: Failed with same error

**Error:**
```
RunPod API Error: 500 - {"error":"create endpoint: create endpoint: graphql: You must have at least $0.01 in your account balance to create an endpoint.","status":500}
```

**Action Required:**
User must add funds to RunPod account (minimum $0.01) to proceed with:
- US-004: Configure vLLM Endpoint
- US-005: Create Audio Serverless Endpoint
- US-006: Create Chatterbox Serverless Endpoint

All endpoint-related stories are blocked until funding is resolved.

---

## US-004: Configure vLLM Endpoint - COMPLETE

**Date:** 2026-01-20 19:49
**Files changed:**
- runpod/vllm/README.md (updated with endpoint details)
- prd.json (updated passes to true)

**Implementation notes:**
- RunPod account now funded - endpoint creation succeeded
- Created endpoint via mcp__runpod__create-endpoint
- Endpoint ID: mo5vt1mlci0mtw
- Endpoint name: yt-vllm (RunPod added "-fb" suffix for flashboot)
- Template ID: gxd9dfcy8l (yt-translate-vllm-qwen)
- GPU: NVIDIA A100 80GB PCIe
- Workers: Min 0 (scale to zero), Max 1
- Idle timeout: 10 seconds
- Flashboot enabled for faster cold starts

**OpenAI-Compatible API:**
- Base URL: https://api.runpod.ai/v2/mo5vt1mlci0mtw/openai/v1
- Endpoints: POST /v1/chat/completions, GET /v1/models

**Template Configuration:**
- MODEL_NAME: Qwen/Qwen3-32B
- MAX_MODEL_LEN: 8192
- GPU_MEMORY_UTILIZATION: 0.95
- TENSOR_PARALLEL_SIZE: 1
- HF_TOKEN: (needs to be set in RunPod dashboard)

**Learnings:**
- RunPod adds "-fb" suffix when flashboot is enabled
- Endpoint creation returns immediately; worker starts on first request
- A100 80GB PCIe sufficient for full Qwen3-32B (no quantization needed)
- vLLM Worker image: runpod/worker-v1-vllm:stable-cuda12.1.0

---

## US-005: Create Audio Serverless Endpoint - COMPLETE

**Date:** 2026-01-20 19:51
**Files changed:**
- prd.json (updated passes to true)

**Implementation notes:**
- Created endpoint via mcp__runpod__create-endpoint
- Endpoint ID: 6ml08xitszksx7
- Endpoint name: yt-audio -fb (RunPod added "-fb" suffix for flashboot)
- Template: yt-translate-audio (k0iwwtx14x)
- GPU: NVIDIA GeForce RTX 4090 (24GB)
- Workers: Min 0 (scale to zero), Max 4
- Note: Max workers reduced from 5 to 4 due to account quota (vLLM endpoint uses 1)
- Idle timeout: 10 seconds
- Flashboot enabled for faster cold starts

**API Details:**
- Endpoint URL: https://api.runpod.ai/v2/6ml08xitszksx7/run
- Status URL: https://api.runpod.ai/v2/6ml08xitszksx7/status/{job_id}

**Learnings:**
- GPU type IDs must use exact strings (e.g., "NVIDIA GeForce RTX 4090", not "RTX 4090")
- Account has max worker quota (5 total) shared across all endpoints
- vLLM endpoint already uses 1 worker, leaving 4 for audio endpoint
- Flashboot enabled by default for faster cold starts

