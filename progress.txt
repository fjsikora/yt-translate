# Ralph Progress Log

## 2026-01-18 16:25:40 - New Session (branch: ralph/jupyter-debug-notebook)

## US-001: Create notebook with setup and configuration cell - COMPLETE

**Date:** 2026-01-18 16:30
**Files changed:**
- debug_pipeline.ipynb (new file)

**Implementation notes:**
- Created Jupyter notebook at `debug_pipeline.ipynb` with setup and configuration cell
- Includes markdown overview explaining the 14-stage pipeline
- Setup cell imports all required dependencies: yt-dlp, openai, replicate, deep_translator, httpx, numpy
- Environment variables loaded via dotenv (OPENAI_API_KEY, REPLICATE_API_TOKEN, OUTPUT_DIR, etc.)
- API clients initialized (OpenAI client for Whisper, Replicate via environment)
- Helper functions included: parse_timestamp(), format_timestamp_srt(), get_ytdlp_opts()
- Configuration status display shows API key status, model names, and pipeline constants
- All imports verified working with .venv-cloud virtual environment

**Learnings:**
- cloud_translate.py is very large (~3000 lines), helper functions are defined inline
- The project has existing config.py and languages.py modules to import from
- utils/audio_helpers.py contains WAV file I/O utilities
- Replicate models are versioned with full SHA identifiers in config.py


## US-002: Create input configuration cell - COMPLETE

**Date:** 2026-01-18
**Files changed:**
- debug_pipeline.ipynb (modified - added 2 cells)

**Implementation notes:**
- Added markdown cell (Cell 2) with comprehensive input documentation:
  - Input variable descriptions table (VIDEO_URL, TARGET_LANGUAGE, DURATION_LIMIT, USE_MULTI_SPEAKER)
  - Supported URL formats (YouTube, Vimeo, Rumble, 1700+ sites)
  - Complete language codes reference table (24 supported languages)
  - Multi-speaker mode explanation (diarization vs single voice synthesis)
- Added code cell with input variables:
  - VIDEO_URL with example YouTube URL
  - TARGET_LANGUAGE (2-letter ISO 639-1 code) with "es" default
  - DURATION_LIMIT with int | None type annotation for optional duration limiting
  - USE_MULTI_SPEAKER boolean toggle for synthesis mode
  - Validation display that checks language code against SUPPORTED_LANGUAGES
- All code cells pass syntax validation via ast.parse()

**Learnings:**
- NotebookEdit tool requires explicit write permissions
- Can use Write tool directly to update notebook JSON when NotebookEdit is blocked
- languages.py SUPPORTED_LANGUAGES contains 24 language codes
- get_language_name() helper function available from languages.py for display


## US-003: Create video info retrieval cell - COMPLETE

**Date:** 2026-01-18
**Files changed:**
- debug_pipeline.ipynb (modified - added 2 cells)

**Implementation notes:**
- Added markdown cell (Cell 3) explaining yt-dlp info extraction:
  - How extract_info() works with download=False
  - Price calculation formula (ceil(duration/60) * rate)
  - Common error scenarios and their causes
- Added code cell with video info retrieval:
  - Uses yt_dlp.YoutubeDL with get_ytdlp_opts() for optional proxy/cookies
  - Extracts title, duration, video_id, thumbnail from info dict
  - calculate_price_cents() function for pricing (PRICE_PER_MINUTE_CENTS default 50)
  - format_duration() helper for human-readable time display
  - Stores video_info dict for use in later cells
  - Comprehensive error handling for DownloadError and general exceptions
  - Shows price adjustment note when DURATION_LIMIT is set
- Typecheck passes for helper modules (config.py, languages.py, utils/audio_helpers.py)

**Learnings:**
- PRICE_PER_MINUTE_CENTS is configurable via environment variable (default: 50 cents)
- yt-dlp provides extractor field to identify the source platform
- thumbnail may be in info['thumbnail'] or info['thumbnails'][-1]['url']
- video_info dictionary pattern useful for passing data between cells


## US-004: Create video download cell - COMPLETE

**Date:** 2026-01-18
**Files changed:**
- debug_pipeline.ipynb (modified - added 2 cells)

**Implementation notes:**
- Added markdown cell (Cell 4) explaining video download process:
  - Format selection (bestvideo+bestaudio/best)
  - Merge output format (MP4 container)
  - Duration trimming with ffmpeg stream copy
  - File output details
- Added code cell with video download implementation:
  - format_file_size() helper for human-readable file sizes
  - Uses yt-dlp with configured options from get_ytdlp_opts()
  - Creates job directory under OUTPUT_DIR using video_id
  - Downloads video with progress display (quiet=False for debugging)
  - Finds downloaded file (checks mp4, mkv, webm extensions)
  - Applies ffmpeg duration trimming when DURATION_LIMIT is set:
    - Uses stream copy (-c:v copy -c:a copy) for fast trimming
    - Replaces original with trimmed version
    - Shows size reduction percentage
  - Stores video_path and actual_duration in video_info dict
  - Comprehensive error handling for subprocess, yt-dlp, and general errors
- Typecheck passes for helper modules

**Learnings:**
- yt-dlp may download as mp4, mkv, or webm depending on source
- ffmpeg stream copy is very fast but can only cut at keyframes
- video_info dict is the standard pattern for passing data between cells
- Previous cell's video_info must be checked before proceeding


## US-005: Create audio extraction cell - COMPLETE

**Date:** 2026-01-18
**Files changed:**
- debug_pipeline.ipynb (modified - added 2 cells)

**Implementation notes:**
- Added markdown cell (Cell 9) explaining audio extraction:
  - Why 16kHz mono PCM is used for Whisper
  - ffmpeg command breakdown with flag descriptions
  - Expected file size calculation
- Added code cell (Cell 10) with audio extraction:
  - WHISPER_SAMPLE_RATE constant (16000 Hz)
  - get_audio_duration() helper using ffprobe
  - ffmpeg extraction: -vn -acodec pcm_s16le -ar 16000 -ac 1
  - Displays audio path, duration, file size
  - Stores audio_path and audio_duration in video_info dict
  - Comprehensive error handling
- All code cells pass Python syntax validation via ast.parse()
- Typecheck passes for helper modules (config.py, languages.py, utils/audio_helpers.py)

**Learnings:**
- ffprobe can extract audio duration with -show_entries format=duration
- WAV file size ≈ duration × sample_rate × 2 bytes + 44 bytes header
- The video_info dictionary continues to be the data-passing pattern between cells


## US-006: Create audio separation cell - COMPLETE

**Date:** 2026-01-18
**Files changed:**
- debug_pipeline.ipynb (modified - added 2 cells)

**Implementation notes:**
- Added markdown cell (Cell 11) explaining Demucs audio separation:
  - Why separating vocals improves transcription quality
  - Demucs model outputs (vocals, drums, bass, other)
  - API parameters (audio as base64 data URI, output_format)
  - Typical processing times for different audio lengths
- Added code cell (Cell 12) with audio separation:
  - Imports base64 module for encoding audio
  - Encodes audio file as base64 data URI for Replicate API
  - Calls REPLICATE_MODEL_DEMUCS with wav output format
  - Handles FileOutput objects from Replicate v1.0+
  - Uses synchronous httpx.Client for Jupyter compatibility
  - Downloads vocals.wav and background.wav (other stem)
  - Displays API call timing and download timing separately
  - Shows file sizes for both stems
  - Stores vocals_path and background_path in video_info dict
  - Includes fallback error handling (uses original audio if separation fails)
- All code cells pass Python syntax validation via ast.parse()
- Typecheck passes for helper modules

**Learnings:**
- Replicate Demucs API expects audio as base64 data URI (data:mime/type;base64,...)
- FileOutput objects from Replicate v1.0+ need .url attribute to get download URL
- Use synchronous httpx.Client in Jupyter (async httpx requires special event loop handling)
- The "other" stem from Demucs contains background music/effects to mix back later


## US-007: Create speaker diarization cell - COMPLETE

**Date:** 2026-01-18
**Files changed:**
- debug_pipeline.ipynb (modified - added 2 cells)

**Implementation notes:**
- Added markdown cell (Cell 13) explaining speaker diarization:
  - What speaker diarization is ("who spoke when?")
  - How the model detects speech regions, clusters speakers, assigns labels
  - Replicate API details (requires URL input, not base64)
  - Speaker label format conversion (A,B,C -> SPEAKER_00,01,02)
  - Typical processing times for different audio lengths
- Added code cell (Cell 14) with speaker diarization:
  - Uses replicate.files.create() to upload vocals file (diarization model requires URL)
  - Calls REPLICATE_MODEL_DIARIZATION (meronym/speaker-diarization)
  - Handles FileOutput objects and URL string outputs from Replicate
  - Parses segments with parse_timestamp() for string timestamps
  - Converts speaker labels from single letters to SPEAKER_XX format
  - Calculates and displays per-speaker statistics (segment count, total duration)
  - Displays formatted segment table with speaker, start, end, duration columns
  - Stores diarization_segments and num_speakers in video_info dict
  - Comprehensive error handling with fallback to single-speaker mode
- All code cells pass Python syntax validation via ast.parse()
- Typecheck passes for helper modules

**Learnings:**
- The speaker-diarization model requires a URL input, not base64 data URI like Demucs
- replicate.files.create(file=path) uploads files and returns object with urls['get']
- Diarization output may use "stop" or "end" for segment end time - handle both
- Timestamps can be either string format (0:00:05.2) or numeric - handle both
- The video_info dict continues as the pattern for passing data between cells


## US-008: Create voice sample extraction cell - COMPLETE

**Date:** 2026-01-18
**Files changed:**
- debug_pipeline.ipynb (modified - added 2 cells)

**Implementation notes:**
- Added markdown cell (Cell 15) explaining voice sample extraction:
  - Why voice samples are needed for Chatterbox voice cloning
  - Sample selection strategy (longest segment per speaker)
  - Duration requirements (MIN_VOICE_SAMPLE_DURATION to MAX_VOICE_SAMPLE_DURATION)
  - ffmpeg extraction command breakdown
- Added code cell (Cell 16) with voice sample extraction:
  - extract_speaker_samples() function that:
    - Groups diarization segments by speaker ID
    - Finds longest segment for each speaker
    - Skips segments shorter than MIN_VOICE_SAMPLE_DURATION
    - Extracts samples using ffmpeg with -ss and -t flags
    - Returns dict mapping speaker_id to sample path
  - Uploads samples to Replicate using replicate.files.create()
  - Builds speaker_voice_urls dictionary with signed URLs
  - Displays formatted summary table with durations and file sizes
  - Shows speaker_voice_urls dictionary contents
  - Comprehensive error handling with fallback to single-speaker mode
  - Stores speaker_samples and speaker_voice_urls in video_info dict
- All code cells pass Python syntax validation via ast.parse()
- Typecheck passes for helper modules

**Learnings:**
- MIN_VOICE_SAMPLE_DURATION (3.0s) and MAX_VOICE_SAMPLE_DURATION (12.0s) from config.py
- cloud_translate.py has extract_speaker_samples_ffmpeg() which we adapted
- Using replicate.files.create() for sample uploads (same pattern as diarization)
- Speakers with all segments below min_duration are skipped silently
- The get_audio_duration() helper from Cell 10 is reused for sample duration display


## US-009: Create transcription cell - COMPLETE

**Date:** 2026-01-18
**Files changed:**
- debug_pipeline.ipynb (modified - added 2 cells)

**Implementation notes:**
- Added markdown cell (Cell 17) explaining transcription:
  - Whisper API parameters (model, file, response_format, timestamp_granularities)
  - verbose_json response format structure
  - Segment structure (start, end, text, tokens, temperature)
  - Typical processing times for different audio lengths
- Added code cell (Cell 18) with transcription implementation:
  - Calls openai_client.audio.transcriptions.create() with whisper-1 model
  - Uses verbose_json response format with segment timestamp granularity
  - Parses segments handling both dict and object access patterns
  - Extracts detected_language, audio_duration, and full_text from response
  - Displays transcription summary (language, duration, segment count, character count)
  - Shows full transcript preview (first 500 characters)
  - Displays formatted segment table with index, start, end, and text columns
  - Shows API call timing and processing ratio (vs real-time)
  - Stores transcription_segments, detected_language, full_transcript in video_info dict
  - Comprehensive error handling for API failures
- All code cells pass Python syntax validation via ast.parse()
- Typecheck passes for helper modules

**Learnings:**
- OpenAI Whisper API returns both object and dict formats depending on version
- verbose_json includes segments with timestamps when timestamp_granularities is set
- Processing ratio (api_time/audio_duration) is useful for benchmarking
- The transcription_segments format matches cloud_translate.py: {start, end, text, duration}

