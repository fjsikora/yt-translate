# YouTube Video Translator Dependencies
# Self-hosted architecture using Self-hosted GPU Serverless

# Environment
python-dotenv>=1.0.0

# HTTP client (for Self-hosted GPU API calls)
httpx>=0.25.0

# Core pipeline
yt-dlp>=2024.0.0
openai-whisper>=20231117
deep-translator>=1.11.0
chatterbox-tts
torchaudio
torch

# Audio processing
demucs>=4.0.0
pyannote.audio>=3.1.0

# Force newer numpy for Python 3.12+ compatibility
numpy>=1.26.0

# TUI
rich>=13.0.0

# Supabase client (database & storage)
supabase>=2.0.0

# Payment provider (payments)
payment-provider>=7.0.0

# System requirement: ffmpeg must be installed separately
